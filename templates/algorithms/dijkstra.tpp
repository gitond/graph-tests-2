#include "../../meta.h"

template <typename graph, typename gVertex, typename path> path dijkstra(graph g, gVertex source, gVertex dest){

	struct {
		gVertex current;
		gVertex parent;
	} examinable;

	examinable.current = source;

	return 0;
}
